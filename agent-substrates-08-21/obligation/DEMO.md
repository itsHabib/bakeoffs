# 60-second demo

Run the one command in the README.

“Every planned worker returned, and the frontier was empty.

Then the oracle recorded a counterexample. The `settled-is-terminal`
mutant consumes the same event bytes and stays empty. The Obligation Engine
reopens exactly one mandatory item:
`resolve_refutation(critical-finding-resolved)`, with reason
`mandatory_obligation_reopened`.

The repair changes the exact subject from H1 to H2. H1 obligations become
superseded, `refresh_subject` opens, and the partial H2 assurance derives only
its named gap: `collect_claim(critical-finding-resolved)`. Old H1
evidence cannot discharge it.

An agent may add an optional diagnostic and it survives replay. An attempt to
remove or downgrade the mandatory finding is refused as
`mandatory_contract_weakened`. Raw receipt input is refused before it can
change the frontier.

The refreshed H2 oracle closes the last gap, so the frontier is empty again.
That is evidence state only. Every displayed assurance still says
`merge_authority: none`; nobody has been authorized to act.”

The JSON that follows is the complete `ObligationFrontierV1` transition
history, its SHA-256 digest, the common bakeoff envelope, the honest scheduler
comparison, and the non-test Go source line count.
