# Purpose-built language entry

Read [`../RULES.md`](../RULES.md) for the common workload, boundaries,
deliverables and assessment. This entry was built independently without
inspecting the other contenders.

The bet: a small language designed around desired resources, work and understandable changes can make lifecycle clearer than generic configuration. Invent only enough syntax for the shared workload, named references and external adapter types. Produce explicit validation errors and a inspectable normalized representation.

Build the six-case common demo and third adapter without editing parser/planner code. Let real behavior establish whether the syntax is useful. Avoid a general programming language, optimizer, package registry, IDE, distributed scheduler or elaborate plugin loader. Explain the ongoing parser/tooling cost honestly.

Finish with runnable code, tests, README, `DEMO.md`, independent correctness review and a draft POC PR. The user judges the approach; report evidence and limits rather than declaring a winner.
