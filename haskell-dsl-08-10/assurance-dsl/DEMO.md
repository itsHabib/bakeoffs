# Demo

Run `stack run`. The demo prints the policy's complete evidence demand, passes
through both Gate and human-judgment alternatives, then refuses otherwise-valid
evidence attached to the previous revision with explicit stale diagnostics.
