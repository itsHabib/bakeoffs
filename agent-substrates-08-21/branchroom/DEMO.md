# 60-second walkthrough

Run `make demo`, then narrate the following while the trace and artifact remain
on screen:

> These runs share a proven declared history, not merely a similar prompt. The
> capsule digest commits to the frozen bundle, contract, repository tree,
> instructions, tool schema, harness, environment, and the same six-event
> prefix.
>
> I fork that prefix into control epoch 13 and counterfactual epoch 14. The only
> declared perturbation turns on the fixture's planted compile-error negative
> control. Each child accepts its own sequence-6 terminal.
>
> Now the race: a delayed sequence-6 result from parent epoch 12 arrives. The
> retain-parent-epoch mutant accepts those same bytes into the child. Branchroom
> refuses them with `parent_epoch_terminal`. Both children refuse at event 7,
> the first event after their common prefix.
>
> The printed `ExperimentForkV1` binds the common prefix, perturbation, branch
> event digests, first divergence, and rejected terminal. Two runs produce the
> byte-identical normalized envelope. A trusted runner could later capture this
> artifact, but it is not evidence, assurance, or authority, and it claims
> nothing about hidden model state.

The demo passes only if the positive terminals are accepted, the mutant accepts
the late parent terminal, production refuses it for the exact epoch reason, the
golden JSON matches, and the disclosed weighted source count stays under 700.
