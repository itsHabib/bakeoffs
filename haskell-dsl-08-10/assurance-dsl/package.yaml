name: assurance-dsl
version: 0.1.0.0
github: itsHabib/bakeoff
license: MIT
ghc-options: [-Wall, -Werror]
dependencies:
  - base >= 4.18 && < 5
  - containers
library:
  source-dirs: src
executables:
  assurance-dsl:
    main: Main.hs
    source-dirs: app
    dependencies: [assurance-dsl]
tests:
  assurance-dsl-test:
    main: Spec.hs
    source-dirs: test
    dependencies: [assurance-dsl]
